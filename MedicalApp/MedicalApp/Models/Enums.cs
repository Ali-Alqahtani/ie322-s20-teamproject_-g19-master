namespace MedicalApp.Models
{
    // GENDER
    public enum Gender
    {
        Male,
        Female
    }

    // AGE
    public enum Nationality
    {
        Saudi,
        Other
    }

    // Status
    public enum Status
    {
        New,
        Cancelled
    }
}
